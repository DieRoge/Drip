# Это приветственная программа на Drip
brew order hello |> 
    order brew name ~> 
        pour ("Hello, " + name) # Склеиваем строки
serve 
    brew order val |> sip serve # Ждем ввода от пользователя
    hello val